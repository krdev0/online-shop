<header>
    <div><a href="#">HandMade Shop</a></div>
    <nav>
        <ul>
            <% if(locals.isAuth) { %>
                <li>
                    <form action="/logout" method="POST">
                        <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
                        <button>Logout</button>
                    </form>
                </li>
            <% } %>
            <li><a href="/login">Login</a></li>
        </ul>
    </nav>
</header>